# CLAUDE.md

Guidance for working on `envoke` — see [README.md](README.md) for the user-facing pitch, use cases, and design rationale. This file is for contributors (human or AI) writing the code.

## Status

MVP steps 1–5 done. Step 1: config parser, path matcher, and executor under `internal/`. Step 2: `envoke shell-init bash|zsh` generates real hook code (`internal/shellinit`). Step 3: `envoke allow` (`internal/trust`) plus the trust check wired into `envoke shell-hook` — a matched block only runs if its config's content hash matches the one recorded by the most recent `allow`; any edit revokes trust until re-approved. Trusted matches are rendered as export+script text (`executor.Render`) that the shell hook `eval`s/`source`s **in the caller's own shell process** — this is deliberately not `internal/envoke.Transition`/`executor.Run` (which exec in a subprocess): only running in the parent shell makes exported vars or `source`d scripts (e.g. the venv-activation use case) actually visible after `cd`. Verified manually end-to-end in real bash and zsh sessions, including that editing a config after `allow` blocks re-execution until re-approved. Step 4: `envoke debug <from> <to>` (`cmdDebug` in `cmd/envoke/main.go`) prints which blocks `matcher.Resolve` would fire for a transition, plus the config's trust status, without ever calling `executor.Run`/`executor.Render` — it works the same whether the config is trusted or not, since its whole point is to let you inspect an unapproved config safely. Step 5: fish/tcsh/powershell shell integration (`internal/shellinit`) plus a shell-aware `executor.Render(shell, leaves, enters)` (a `--shell <name>` flag on `envoke shell-hook`, defaulted to POSIX so existing bash/zsh installs are unaffected) — each shell speaks a different "export a variable"/quoting dialect, and tcsh's `cwdcmd` special alias turned out to run through a restricted execution path that silently drops `|`/`>` unless wrapped in `eval` (a real bug caught only by driving a real tcsh end to end, not by string-matching the generated script — see `internal/shellinit`'s tcshHook doc comment before touching it). `go test ./... -race` passes across all packages (102 tests); `gofmt`/`go vet` clean. Step 6: `.dagger/` is a self-contained Dagger module (its own `go.mod`, targeting the Go SDK's "next"/1.0-beta `+check` function convention) exposing `fmt`/`vet`/`build`/`test`/`lint` checks plus `test-shell-{bash,zsh,fish,tcsh,powershell}` — one check per shell, each installing only that interpreter, so `go test ./internal/shellinit/...`'s `requireInterpreter` gating actually exercises every shell instead of skipping the four not present on a given dev machine. Running the full matrix for the first time (`dagger check -m .dagger`) caught two real bugs the local dev machine's tooling had been masking: (1) `internal/shellinit`'s `tcshHook` invoked `command envoke ...` to bypass a user's same-named alias, but tcsh has no `command` builtin — it happened to work during prior manual testing only because macOS ships an actual `/usr/bin/command` executable; Debian doesn't. Fixed to tcsh's native alias-bypass syntax, `\envoke`. (2) `TestGenerate_PowershellScriptIsSyntacticallyValid` fed `$input` (a line-by-line pipeline enumerable) to `Parser::ParseInput`, silently collapsing the generated script's newlines into one run-on line and failing to parse; fixed to read stdin as one raw string via `[Console]::In.ReadToEnd()`. Neither bug was visible before because fish/pwsh weren't installed locally and this was the first time either ran end to end. `dagger check -m .dagger` now also runs two GitHub-Actions-focused checks, `zizmor` (static security audit of `.github/workflows/*.yml` via `ghcr.io/zizmorcore/zizmor:latest`) and `actions-up` (checks every `uses:` is SHA-pinned and at its latest version, via `npx actions-up`) — same philosophy as [sensiolabs/GotenbergBundle#284](https://github.com/sensiolabs/GotenbergBundle/pull/284), adapted to this module's flatter, single-file style (no per-tool `DaggerObject`s) and to the Go SDK's own ergonomics. Both tools read an optional `GhAuthToken *dagger.Secret` on `Envoke` (a `*dagger.Secret`/other Dagger object-typed parameter is nullable by the Go SDK's codegen convention already, but the CLI still requires it to be passed unless the parameter carries an explicit `// +optional` doc-comment directly above it — found only by trying it and getting `missing required argument`, not by reading the codegen source) — `ZIZMOR_GITHUB_TOKEN`/`GITHUB_TOKEN` respectively, raising both tools' unauthenticated 60/hr GitHub API rate limit; local runs reuse the caller's own `gh` session rather than minting a PAT: `dagger -m .dagger call --gh-auth-token='cmd://gh auth token' zizmor`. Checks stay one function per tool (`Zizmor`, `ActionsUp`), but autofixing is a single unified `Autofix` function rather than one per tool (deliberate: a contributor fixing CI wants one command, not two) — it chains zizmor's `--fix=all` into actions-up's `--yes` pass over the zizmor-fixed tree and returns the combined `*dagger.Changeset`, applied locally via `dagger -m .dagger call autofix export --path=.`. The zizmor step in `Autofix` must pass `Expect: dagger.ReturnTypeAny` to `WithExec`, not the default success-only expectation — `zizmor --fix=all` exits non-zero (14) whenever findings remain after fixing, even when every requested fix was applied cleanly, because some findings need the riskier `--fix=unsafe` (deliberately not passed) or can't be auto-fixed at all. Both checks were verified against the repo's own **untouched** `release.yml`/`docs.yml` before any manual edit, specifically to prove detection worked rather than assume it: zizmor found 16 findings (unpinned `uses:` on every action, plus `actions/checkout`'s missing `persist-credentials: false`), actions-up found 6 pending version bumps. Running `Autofix` resolved all of those, but left one zizmor finding `Autofix` cannot touch — `docs.yml`'s `pages: write`/`id-token: write` declared at the workflow level rather than scoped to the one job (`deploy`) that needs them (`excessive-permissions`, no auto-fix available) — fixed by hand by moving those two lines into `deploy`'s own `permissions:` block, confirmed with a second `zizmor` run. `.github/workflows/ci.yml` now runs `dagger check -m .dagger` on push/PR to `main` and on a daily cron (so a dependency going stale surfaces even without a push). This closes the gap step 6 originally left open (checks existed but nothing ran them in CI) — and the first real push caught two more real bugs, exactly like the local-dev-vs-CI gaps step 6 itself already found: (1) `dagger/dagger-for-github`'s `call:` input runs `dagger call <value>`, and `check` is not a `dagger call` function — it's a separate top-level CLI verb (`dagger check`), only reachable through the action's own dedicated `check:` input (any non-empty value; the action discards it and just switches its internal verb to `check`), so `call: check` failed with `unknown command "check" for "dagger call"`. (2) unlike `call`, the `check` verb doesn't forward extra flags to the module constructor at all — `dagger check -m .dagger --gh-auth-token=...` fails with `unknown flag: --gh-auth-token` even locally — confirmed by fetching the pinned `dagger-for-github` action's actual source (`action.yml`): its `check:` input is used purely as a trigger and its value is explicitly discarded (`check=""`) before the CLI ever runs, by design ("don't pass glob patterns as args"). CI initially ran `zizmor`/`actions-up` unauthenticated as part of that aggregate `check: "true"` and was believed fine at this repo's small scale — until a real burst of Renovate-triggered CI runs on the same day genuinely exhausted the 60/hr unauthenticated GitHub API limit and failed a PR's `check` job with `Rate Limit Exceeded` (not a code problem, confirmed by reading the actual job log). Fixed properly, not by tolerating the flakiness: `Zizmor` and `ActionsUp` had their `+check` pragma removed entirely (they're plain functions now, callable only via `dagger call`) — `dagger check -m .dagger` (bare `check: "true"` in CI, no manually-maintained include/exclude list) now only ever runs the ten checks that never needed a token, and can never drift out of sync since it just runs "whatever is `+check`-annotated," full stop. `ci.yml` gained two more `dagger-for-github` steps, `call: --gh-auth-token=env://GITHUB_TOKEN zizmor` and `call: --gh-auth-token=env://GITHUB_TOKEN actions-up`, each with `env: GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}` — the constructor flag has to precede the function name in `call`'s argument string, matching the already-proven local usage (`dagger -m .dagger call --gh-auth-token=... zizmor`). No new secret needed: the default per-run `GITHUB_TOKEN` every GitHub Actions job already has was simply never being passed through before. `actions-autofix.yml`'s `call: autofix export --path=.` step had the identical gap (no token at all, so its own daily zizmor/actions-up analysis was also unauthenticated) and got the same fix: `call: --gh-auth-token=env://GITHUB_TOKEN autofix export --path=.`. (Aside, explored and rejected: `dagger check` does support wildcard patterns as positional args, e.g. `dagger check -m .dagger "envoke:test*"` matches `test` + all five `test-shell-*` checks at once — confirmed working locally — but there's no negation/exclude syntax, so selecting "everything except zizmor/actions-up" via CLI args would still mean hand-maintaining an include-list in `ci.yml` that silently goes stale the day a new `+check` function is added; removing the pragma from the two token-needing functions sidesteps that class of bug entirely rather than working around it.) Step 7 (packaging) is partially started: `.dagger/main.go` adds two goreleaser-backed functions on top of the same module — `Snapshot` (`dagger call -m .dagger snapshot export --path=...`) runs `goreleaser release --snapshot --clean` (cross-compiles envoke for linux/darwin/windows × amd64/arm64 per `.goreleaser.yaml`, archives, and checksums, entirely inside a `goreleaser/goreleaser:v2.17.0` container) and returns the `dist/` directory — `--snapshot` implies `--skip=announce,publish,validate`, so this never touches GitHub and is safe to run anytime. `Publish` runs `goreleaser release --clean` with a `GITHUB_TOKEN` secret to actually cut a GitHub Release for the checked-out tag. `.github/workflows/release.yml` wires this up: on a pushed `v*` tag, it checks out full history (`fetch-depth: 0`, goreleaser needs the tag/changelog history) and runs `dagger/dagger-for-github@v8.4.1` (pinned — no `v8` floating major tag exists on that action yet, only `v7` and versioned `v8.x.y` releases) with `call: publish --github-token=env://PUBLISH_HOMEBREW_TAP`. `PUBLISH_HOMEBREW_TAP` is a repo secret holding a PAT scoped with write access to *both* `envoke` and `homebrew-tap` — the default Actions `GITHUB_TOKEN` only has access to the repo the workflow runs in, which isn't enough for the cross-repo tap push below, so this one token is used as `Publish`'s `githubToken` for both the release itself and (via goreleaser's fallback — `.goreleaser.yaml`'s `homebrew_casks.repository` has no `token:` override, so it reuses the same `GITHUB_TOKEN`) the tap push. **Exercised for real, twice**: `v0.1.0` was first cut with the (by-then-already-deprecated) `brews:` key to prove the pipeline end to end, then the release + tag were deleted (`gh release delete v0.1.0 --cleanup-tag`) and `v0.1.0` was regenerated from scratch after migrating to `homebrew_casks:` and adding the install/upgrade footer (both described below) — same GitHub Git Data API tag-creation approach (not `git tag`/`git push --tags` locally), firing the same `push: tags:` event. [The current run](https://github.com/Neirda24/envoke/actions/runs/29401322248) built and published all six archives + `checksums.txt` to [the v0.1.0 release](https://github.com/Neirda24/envoke/releases/tag/v0.1.0), pushed `Casks/envoke.rb` to `Neirda24/homebrew-tap`, and the release notes render the footer correctly (`go install github.com/Neirda24/envoke/cmd/envoke@v0.1.0`, `.Tag` substituted for real) — confirming secret, cross-repo token, cask push, and footer templating all work unattended. The old `Formula/envoke.rb` (from the original `brews:`-based run), orphaned by the cask migration, has since been deleted from `Neirda24/homebrew-tap` directly via the GitHub API — per goreleaser's own deprecation note ("you might also want to delete the old Formulas from your Tap") — so the tap now only contains `Casks/`. `goreleaser` itself is never installed on the host — both functions run it only inside the Dagger container, per the "one binary, no third-party deps on the dev machine" goal. Homebrew: `Neirda24/homebrew-tap` now exists (a generic, multi-project tap rather than one repo per tool). `.goreleaser.yaml` originally used a `brews:` entry, which goreleaser v2.17.0 flags as deprecated (soft since v2.10) in favor of `homebrew_casks:` — migrated to the latter (`directory: Casks`, `binaries: [envoke]`, a `postflight` hook running `xattr -dr com.apple.quarantine` on the staged binary, guarded by `if OS.mac?` since it'd error on Linux where `xattr` doesn't exist). This quarantine-removal hook is standard practice for an unsigned Homebrew cask — it's what lets `brew install` skip the Gatekeeper "unidentified developer" prompt a manually-downloaded unsigned binary would otherwise trigger. Combined with `checksums.txt` now being cosign-signed (see the goreleaser `signs:` block and `.dagger/main.go`'s `Publish` below), a `brew install`/`brew upgrade` gets some integrity assurance the checksum-signing chain provides, but the `envoke` binary itself is still not code-signed or notarized — full Apple notarization is a real future hardening step, not done here because it needs a paid Apple Developer account and isn't justified yet at this project's scale. Despite "cask" in the name, modern Homebrew Cask supports `on_macos`/`on_linux` blocks — goreleaser auto-generates both from the `linux`/`darwin` builds already in this config, so this migration does *not* drop Linux `brew install` support. `release.footer` (Go-templated, appended after the auto-generated changelog) adds an "Install / upgrade" section to every release's notes — Homebrew, manual download from Assets, and `go install .../cmd/envoke@{{ .Tag }}` — see the Don't section for why this is enforced structurally rather than added by hand per release. Scoop bucket / nfpm deb-rpm are still not in `.goreleaser.yaml` — same reasoning, add once/if those targets exist. `renovate.json` (root) handles dependency updates: `config:recommended` covers both `go.mod`s (root + `.dagger/`) and every `.github/workflows/*.yml` `uses:` pin natively, plus `docs/requirements.txt` (`pip_requirements` manager) for `mkdocs-material`. Four `customManagers` (regex) cover the version pins Renovate can't see by default: `.dagger/dagger.json`'s `engineVersion` and the duplicate `version: "0.21.7"` passed to `dagger-for-github` in both `ci.yml` and `release.yml` are grouped into one PR (same `datasource`/`depName`, per a `packageRules` group) since they're meant to move together but aren't otherwise linked; `golangciLintPath`'s `@v2.12.2` pin (`go` datasource, since it's a `go install` target, not a `go.mod` dependency); and the four Docker base images in `.dagger/main.go`'s `const` block, each flagged with a `// renovate: datasource=docker` comment the regex anchors on (`zizmorImage` deliberately excluded — it tracks `:latest`, not a pinned tag). All five regexes were verified by hand against the real file contents (each captures the expected `depName`/`currentValue`) and `renovate-config-validator` passes, but the config itself hasn't been exercised end-to-end yet — that needs the Renovate GitHub App enabled on the repo, which is a manual step outside what a config file can do. The four Docker-image consts are pinned to both tag *and* digest (`image:tag@sha256:...`, e.g. `golang:1.23-bookworm@sha256:167053a2...`) — the digests were fetched once via `docker buildx imagetools inspect <image>` and hand-verified to pull (`docker pull` on the combined ref) since Dagger's `Container().From()` parses standard OCI refs the same way Docker does. The docker regex manager's `currentDigest` capture group is what tells Renovate to keep the digest in sync going forward: on a tag bump it resolves+writes the new tag's digest, and on a same-tag rebuild (digest-only change, e.g. `golang:1.23-bookworm` getting security patches) it opens a digest-only PR — this is the same mechanism behind Renovate's `docker:pinDigests` preset, just driven manually here since these are Go string constants, not a real Dockerfile the native docker manager could see. `.github/workflows/actions-autofix.yml` runs `dagger call autofix export --path=.` daily (`0 2 * * *`, ahead of whatever cadence Renovate ends up on) and opens a PR (`actions-autofix/<date>` branch, via `gh pr create`) if zizmor/actions-up found and applied anything — this is deliberately *not* wired through Renovate's `postUpgradeTasks` (chaining a command into Renovate's own PR), because that feature is admin-only on self-hosted Renovate and silently does nothing on the standard hosted GitHub App, which this repo isn't committed to one way or the other yet. Running autofix on its own daily schedule gets the same practical effect without depending on that choice: whichever Renovate deployment eventually gets installed, the github-actions manager (on by default via `config:recommended`, left enabled — no exclusion needed) only ever proposes what that day's autofix run didn't already catch, since both are idempotent against an already-fixed file. The job's own checkout needs `persist-credentials: false` (job-wide credential persistence is what zizmor's `artipacked` audit flags — confirmed by first getting the finding, then clearing it with this fix) with the push authenticated per-command instead, via a scoped `http.extraheader` built from `github.token`; `dagger call zizmor`/`dagger call actions-up` were both run against the new workflow file itself to confirm it introduces no findings before this was considered done. **Renovate was installed for real** (GitHub App, repo-scoped) and its first Dependency Dashboard run confirmed most of the design: the `.dagger/main.go` docker images grouped correctly and updated tag+digest together (`golang`/`goreleaser`/`python` batched into one PR since none were major bumps; `node:22-alpine`→`24-alpine` split into its own PR since Renovate treats that as a major version — expected, not a bug, since majors get extra scrutiny by default). But `.dagger/go.mod`'s two dependency PRs (`gqlparser`, the `opentelemetry-go` monorepo) both failed with a real "Artifact update problem": `go get -t ./...` errors because `.dagger`'s module imports `dagger/envoke/internal/dagger`, the generated, gitignored package that only exists locally after `dagger develop -m .dagger` runs — Renovate's sandboxed checkout never has it. Fixed by disabling the `gomod` manager entirely for `.dagger/**` via a `packageRules` entry — not just because Renovate can't compute `go.sum` there, but because `.dagger/go.mod`'s dependency versions are dictated by the Dagger Go SDK's codegen for whatever `engineVersion` is pinned in `.dagger/dagger.json`, and are only meant to move via `dagger develop`, not piecemeal Renovate bumps. `envoke allow` gained a real confirmation gate: previously `printBlocksForReview`'s output literally said "review each block below before confirming," but `cmdAllow` never read stdin at all — it called `trust.Allow` unconditionally right after printing the dump, so the word "confirming" was a lie and there was no way to review-then-decline. Fixed properly, not just documented around: `cmdAllow` (`cmd/envoke/main.go`) now reads a line from stdin by default and only proceeds to `trust.Allow` on `y`/`yes` (case-insensitive); anything else — a different answer, an empty line, or EOF — prints `envoke: aborted, not trusted` to stderr and exits 1 without trusting. A new `--yes`/`-y` flag (recognized anywhere among `allow`'s args, before or after the optional path) skips the prompt for scripts/CI, restoring the old unconditional behavior as an opt-in rather than the default; `run`'s signature grew a `stdin io.Reader` parameter threaded through from `main` (`os.Stdin`) so the prompt is exercisable in tests without touching the real process stdin. Alongside this, `internal/trust` gained `PreviousContent(configPath) (content string, ok bool, err error)`, and `Allow` now writes the approved content itself to a new sibling file (`<hash-record-path>.content`) alongside the existing hash record — deliberately additive, not a format change to the existing record, specifically so upgrading envoke never revokes a pre-existing user's trust: a hash-only record from before this change is treated by both `IsTrusted` and `PreviousContent` as a normal, valid state (the latter just reports `ok=false`, "no prior content to diff against"), not corruption. This unlocked a second, related fix in `cmdAllow`: re-running `envoke allow` on an already-trusted config now branches three ways depending on `PreviousContent` — no prior content (first-ever trust) still gets the full `printBlocksForReview` dump; prior content identical to the current file byte-for-byte skips both the dump and the y/N prompt entirely, printing `envoke: <path> is unchanged since it was last trusted -- nothing to review` and returning immediately (re-recording an unchanged hash would be pure busywork); prior content that differs shows a labeled `+`/`-` line diff (new `printDiff`/`diffLines` functions) against the previously-approved content instead of re-dumping the whole file, so reviewing a one-line edit to an already-trusted config doesn't require re-reading everything. `diffLines` is a stdlib-only O(len(old)×len(new)) LCS (longest-common-subsequence) diff, not a full Myers diff — config files are small enough that the simpler DP table is both correct and fast, and needed no new dependency. Separately, `envoke version` was fixed for a genuine pre-existing bug, not just enhanced: `.goreleaser.yaml`'s `envoke` build had no `ldflags` block at all, so `main.go`'s `version`/`commit`/`date` vars kept their zero-value defaults (`"dev"`/`"unknown"`/`"unknown"`) in every released binary ever built by CI, including the real `v0.1.0` release described above — every user who ran `envoke version` against a genuine tagged release has been seeing `envoke dev` this whole time. Fixed by adding `ldflags: [-s -w -X main.version={{.Version}} -X main.commit={{.Commit}} -X main.date={{.Date}}]` to the build block; `printVersion` was also expanded while fixing this to print a second line with `runtime.Version()`/`GOOS`/`GOARCH`, since both pieces of information are useful for the same bug-report use case and cost nothing extra to add alongside the ldflags fix. Follow the MVP scope below rather than building everything in the design notes at once.

## Design principles (non-negotiable, see README § Design notes)

- **RE2 matching only** — use Go's standard `regexp`. Never shell out to or vendor a backtracking regex engine; that reintroduces ondir's catastrophic-backtracking hangs.
- **Path-segment matching, not string-prefix matching** — a pattern must match whole path segments. Never implement matching as a raw `strings.HasPrefix`.
- **Enter/leave are independent and explicit** — no automatic state snapshot/restore on leave. Don't add "smart" auto-undo; if that's ever wanted, it's a deliberate opt-in feature, not a default.
- **Trust before execution** — any new or modified config must go through an explicit `envoke allow` before its blocks run. Never auto-execute an unapproved config, even in convenience-focused code paths.
- **One binary generates all shell integration** — hook scripts for bash/zsh/fish/tcsh/powershell are generated by the binary (`envoke shell-init <shell>`), not maintained as separate hand-written files per shell.

## MVP scope order

1. ✅ Config parser + path matcher + `enter`/`leave` execution (core loop only).
2. ✅ Shell hook generation for bash and zsh (hook plumbing + match reporting).
3. ✅ `envoke allow` trust mechanism — trusted matches now actually execute via `executor.Render` + the shell's own `eval`.
4. ✅ `envoke debug <from> <to>` dry-run diagnostics — reports matches and trust status, never executes.
5. ✅ fish/tcsh/powershell shell integration — hook generation plus a shell-aware `executor.Render`.
6. ✅ CI checks via Dagger (`.dagger/` module) — `dagger check` mirrors CONTRIBUTING.md's four manual commands plus golangci-lint, and runs the shellinit end-to-end tests against a real interpreter for each of the five supported shells.
7. 🚧 Packaging (goreleaser: binaries, Homebrew tap, Scoop bucket, nfpm deb/rpm) — binaries, GitHub Release, Homebrew tap, and tag-triggered release CI all done and verified end to end against a real `v0.1.0` tag (see Status); Scoop bucket / nfpm still pending.

Don't jump ahead to later stages before earlier ones have tests passing.

## Architecture sketch

Current package layout (`internal/`, not importable outside this module — there's no stable public API to commit to yet):

- **`internal/config`** — `Parse`/`ParseFile` turn a config file into `[]Block{Type, Pattern, RawPattern, Script, Line}`. Hand-rolled line-oriented parser (see decision below), not a grammar library. Pattern compilation (`pattern.go`) expands a leading `~` and `$VAR`/`${VAR}` as literal (`regexp.QuoteMeta`'d) substitutions, then wraps the result as `^(?:...)$` before `regexp.Compile` — that anchoring is what makes matching segment-based rather than prefix-based (fixes ondir's `/home/foo` vs `/home/foobar` bug structurally, not by convention). Malformed config fails with a positioned `*ParseError{Line, Msg}`, never silently.
- **`internal/matcher`** — `Transitions(from, to)` walks both paths' ancestor chains via `filepath.Dir` to their common ancestor, returning directories left (deepest-first) and entered (shallowest-first) — this is ondir's traverse behavior: jumping straight from `/a` to `/a/x/y/z` still fires `/a/x` and `/a/x/y`'s rules. `Resolve(cfg, from, to)` runs every block's pattern against the relevant directories and returns ordered `[]Match`.
- **`internal/executor`** — two execution models sharing `matchVars` (the `ENVOKE_DIR`/`ENVOKE_TYPE`/`ENVOKE_MATCH`/`ENVOKE_MATCH_N` a matched block sees):
  - `Run(ctx, match)` execs the script via `sh -c` as a **subprocess** (`cmd.Dir` set to the matched directory, vars as env, inherited stdio). Used by `internal/envoke.Transition`; side effects like `export`/`source` don't escape this subprocess, so this path is for non-interactive/one-off execution, not the shell hook.
  - `Render(shell, leaves, enters)` builds ENVOKE_*-var-assignment-prefixed shell text for every match, in the same leave-then-enter order, meant to be `eval`'d/`source`d by the *caller's own shell*. This is what `cmd/envoke shell-hook` uses once a config is trusted — it's the only path where `export`/`source` in a script actually affects the user's interactive shell. `shell` selects a `shellProfile` (posix `export VAR=...`, fish `set -gx`, tcsh `setenv`, or PowerShell `$env:VAR = ...`) — an unrecognized name falls back to POSIX. Each profile has its own quoting function (`posixQuote`/`fishQuote`/`powershellQuote`; tcsh reuses `posixQuote`, confirmed against a real tcsh) so directory names or capture groups with spaces/`$`/backticks/embedded quotes can't break out of the generated code — regression-tested by piping `Render`'s output through a real `sh -c` (POSIX) and a real `tcsh -c`. Render can only translate the ENVOKE_* plumbing, not the matched block's own script text — a script body still needs to actually be written in the calling shell's syntax to behave when eval'd by fish/tcsh/powershell, same as it always implicitly needed to be POSIX-ish for bash/zsh.
- **`internal/envoke`** — `Transition(ctx, cfg, from, to)` is the subprocess-based core loop: resolve, then run all leaves, then all enters via `executor.Run`, stopping at the first failing block (no partial-transition unwind — see the enter/leave independence principle above). Exercised end-to-end by its own integration tests. Not used by the shell hook (see `executor.Render` above) or by `envoke debug` (which never executes anything) — it's there for non-interactive automation where "eval in the parent shell" doesn't apply.
- **`internal/config.Locate()`** — resolves the config path: `$ENVOKERC` (used verbatim, even if missing) → `~/.envokerc` if present → `$XDG_CONFIG_HOME/envoke/config` (or `~/.config/envoke/config`) if present → not found (normal state, not an error).
- **`internal/trust`** — `Allow(path)` / `IsTrusted(path)` / `PreviousContent(path) (content string, ok bool, err error)`. Trust is a SHA-256 hash of the config file's *content*, recorded under `$XDG_DATA_HOME/envoke/allow/<sha256(abs path)>` (or `~/.local/share/...`) — a record file per config path, so distinct configs never collide. `IsTrusted` recomputes the current content hash and compares; any edit — even whitespace — changes the hash and revokes trust until `Allow` runs again. `Allow` also persists the approved content itself to a sibling `<record>.content` file, which `PreviousContent` reads back — additive, so a pre-upgrade hash-only record is a normal state (`ok=false`), not an error. This is the concrete mechanism behind the non-negotiable trust-before-execution principle below; `shell-hook` calls `IsTrusted` before ever calling `executor.Render`, and `cmdAllow` calls `PreviousContent` to decide whether to show a full block dump or a diff (see Status).
- **`internal/shellinit`** — `Generate(shell)` returns the literal hook script for `"bash"`, `"zsh"`, `"fish"`, `"tcsh"`, or `"powershell"` (static strings, no templating needed since there's no per-user variation). Every hook calls `envoke shell-hook` with a shell-specific `--shell <name>` flag so `executor.Render` picks the right export dialect. Each hook's mechanism, in increasing order of how many real-interpreter gotchas it cost to get right:
  - **bash** has no native "on cd" hook, so it polls via `PROMPT_COMMAND` (comparing `$PWD` to a var saved after the last prompt) — **the baseline var must be seeded once at hook-install time**, not lazily inside the hook function, or the very first `cd` after installing the hook compares the new directory against itself and gets silently missed (`TestGenerate_BashHookFiresOnFirstCd`, driving real bash/`PROMPT_COMMAND` end to end).
  - **zsh** uses the native `chpwd_functions` array, which only fires on a real `cd` and always has `$OLDPWD` already set by the shell itself, so it needs no seeding trick.
  - **fish** uses the native `--on-variable PWD` event. Fish's own `$OLDPWD` support isn't reliable enough to depend on, so — like bash — the hook tracks and seeds its own baseline var. A bare command substitution in fish splits multi-line output into one list element per line, so `string collect` (fish 3.4+) gathers `envoke shell-hook`'s stdout into one string before `eval`, or a multi-line matched script would silently become several unrelated single-line evals.
  - **tcsh** uses the special `cwdcmd` alias (csh's native "run after every cd/pushd/popd" hook — tcsh maintains `$owd`/`$cwd` itself, no seeding needed), claiming that slot outright rather than chaining with a pre-existing user `cwdcmd` (a documented tcsh idiom, e.g. xterm title-setting) — undocumented limitation, noted in the tcshHook doc comment. Three real bugs found only by driving a real tcsh end to end (string-matching the generated script caught none of them): (1) tcsh has no `export`/`VAR=value` syntax, and both plain and quoted backquote command substitution split multi-line output on newlines, so instead of capturing-then-`eval`, the hook pipes `envoke shell-hook`'s stdout straight into `source /dev/stdin`; (2) `cwdcmd`'s body runs through a restricted internal execution path that does **not** honor `|`/`>` directly — a bare `cmd | source /dev/stdin` inside the alias silently prints to the terminal instead of piping, and a `setenv` inside it never reaches the interactive shell. Wrapping the whole thing in `eval "..."` forces a full re-parse that does honor `|`, and `$owd`/`$cwd` are single-quoted *inside* that eval string (not just around the whole alias) so a directory with a space still reaches `shell-hook` as one argument; (3) tcsh has no `command` builtin — the hook originally used `command envoke ...` (copying the bash/zsh/fish idiom for bypassing a same-named user alias) and this worked on macOS purely by accident, because macOS ships a real `/usr/bin/command` executable; on Linux tcsh it fails outright with "command: Command not found." Fixed to tcsh's own alias-bypass syntax, `\envoke`. `TestGenerate_TcshHookSetenvPersistsInCallingShell` is the regression test for (1)/(2) — note it doesn't just check `envoke` was *called* correctly (a weaker test that already existed passed even with both bugs present, since the call itself succeeded and only the piped/setenv side effect was silently lost). Bug (3) only surfaced once the Dagger CI matrix (step 6, below) ran this suite against Linux's tcsh for the first time.
  - **powershell** wraps the `prompt` function (PowerShell's idiomatic customization point — what posh-git/oh-my-posh also use), saving and always calling through to whatever `prompt` was previously defined, guarded by an installed-flag against double-wrapping if sourced twice. `Out-String` joins `envoke shell-hook`'s output into one string before `Invoke-Expression`, the same multi-line concern as fish's `string collect`.

  None of the five hooks redefine `cd` (regression-tested via `assertNeverRedefinesCd`).
- **`cmd/envoke`** — `version` (prints version/commit/build-date, via ldflags injected by `.goreleaser.yaml` at release time, plus `runtime.Version()`/`GOOS`/`GOARCH` — see Status for the pre-existing bug this fixed); `shell-init <bash|zsh|fish|tcsh|powershell>` (prints the hook script); `allow [--yes|-y] [path]` (defaults to `config.Locate()`, refuses to trust a config that doesn't even parse; shows a full block dump on first-ever trust or a `+`/`-` line diff against `trust.PreviousContent` on a re-allow after an edit, or nothing at all if the content is unchanged since last trusted; by default then reads a `y`/`yes` confirmation from stdin before calling `trust.Allow`, aborting on anything else — `--yes`/`-y` skips the prompt); `shell-hook [--shell <name>] <from> <to>` (invoked by the installed hook on every `cd` — resolves matches via `matcher.Resolve`, checks `trust.IsTrusted`, and only then prints `executor.Render(shell, ...)`'s output to stdout; an untrusted match is reported on stderr only, with an `envoke allow <path>` hint, and nothing goes to stdout; omitting `--shell`, as bash's/zsh's generated hooks do, defaults to the POSIX profile); `debug <from> <to>` (same `matcher.Resolve` call as shell-hook, but always prints what would fire — plus whether the config is currently trusted — to stdout and never calls `executor.Run`/`executor.Render`, so it's safe to run against an unapproved or half-written config). `run(args, stdout, stderr, stdin) int` is the testable dispatcher `main()` wraps; `stdin` exists solely for `allow`'s confirmation prompt.
- **Shell integration**: `envoke shell-init <shell>` prints shell code to `eval`/`source`, hooking the shell's own directory-change mechanism (`chpwd_functions` for zsh, `PROMPT_COMMAND` polling for bash, `--on-variable PWD` for fish, `cwdcmd` for tcsh, a wrapped `prompt` function for PowerShell) — never override `cd` directly.
- **`.dagger/`** — a separate Go module (own `go.mod`, `dagger/envoke`) holding the CI pipeline; not imported by and not a dependency of the main `github.com/Neirda24/envoke` module, so the zero-non-stdlib-dependency rule for the product code is untouched. `main.go` defines checks (Go SDK `// +check` doc-comment pragma, run via `dagger check -m .dagger`): `fmt`/`vet`/`build`/`test` mirror CONTRIBUTING.md's four manual commands exactly; `lint` runs golangci-lint (pinned `v2.12.2`, installed with `GOTOOLCHAIN=auto` since it needs a newer Go than this module's `go.mod` floor); `test-shell-{bash,zsh,fish,tcsh,powershell}` each build a container with exactly one shell installed (zsh/fish/tcsh via `apt-get`, powershell via Microsoft's Debian apt repo, bash already in the base `golang` image) and run `go test ./internal/shellinit/...` in it — a check can't take arguments (Dagger checks are always argument-free), which is why this is five separate check functions sharing a private helper rather than one parameterized check. `dagger.gen.go`/`internal/dagger`/`internal/telemetry` under `.dagger/` are generated-on-demand and gitignored by `dagger init`'s own `.gitignore` — don't hand-edit or commit them; regenerate with `dagger develop -m .dagger` after changing `New`'s signature (needed whenever a constructor-level parameter like `GhAuthToken` is added or changed, since `dagger.gen.go`'s `main()` glue calls `New` positionally). `zizmor`/`actions-up` (`+check`) and `autofix` (plain function, deliberately not a check — see Status) round out `main.go`; `zizmorBase`/`actionsUpBase` are the equivalent of `goBase` for those two tools, both taking the optional `Envoke.GhAuthToken`. `.github/workflows/ci.yml` runs `dagger check -m .dagger` on push/PR to `main` and a daily cron — see Status for what the first real run against this repo's own workflows caught.

## Go conventions

- **CLI framework: stdlib `flag`**, decided at step 1. No subcommands existed yet to justify `cobra`/`urfave-cli`'s added dependency; revisit once step 2 (shell-init) or step 3 (allow) needs real subcommand parsing.
- **Config parser: hand-rolled, not participle.** The grammar is a simple line-oriented format (unindented `enter`/`leave` header, indented dedented body) — a hand-rolled scanner keeps positioned error messages straightforward and avoids a dependency for something this small. Revisit only if the grammar grows real nesting/expressions.
- Table-driven tests via subtests (`Test<Func>_<Scenario>` naming), including regression tests for each ondir bug listed in the README's Design notes table (basename-prefix false positives, `~`/env-var expansion, capture-group exposure, traverse behavior). `go test ./... -race` must pass.
- **Never run `go build`/`go test`/`go vet`/`gofmt`/`golangci-lint` (or any other verification) directly on the host machine.** Always verify through `dagger call -m .dagger <check>` (`fmt`/`vet`/`build`/`test`/`lint`/`test-shell-*`) — this runs on the exact pinned images CI uses, not whatever happens to be installed on a given dev machine, which is the whole point of the `.dagger` module (see Status). This also applies to the `.dagger` module's own code (own `go.mod`, so the root checks' `./...` never reaches it, and `gofmt` skips dot-directories entirely) — verify it via `docker run` against the pinned `goImage`, the same one-off pattern as below. For a one-off verification that doesn't belong as a permanent `.dagger` check — e.g. running the full `internal/shellinit` suite with every interpreter installed at once so nothing `t.Skip`s, which the five `test-shell-*` checks deliberately don't do (each isolates one shell per container on purpose, see Status) — use `docker run` directly against a scratch container instead of adding a throwaway concern into `.dagger/main.go`; `scripts/test-shellinit-all-shells.sh` is exactly that, checked in so it's a rerunnable command rather than something to retype by hand each time. No test run should ever end in a skip that a real interpreter could have resolved — if `go test` output shows a `SKIP`, that's a signal to run it through Dagger/Docker instead, not something to wave off as expected.
- Static, dependency-free binaries — avoid cgo. Zero non-stdlib imports so far; keep it that way unless a real need appears.
- Generated shell code gets tested with the real interpreter, not just string-matching: `<shell> -n -c <script>` for a syntax check, and for behavioral bugs (like the bash first-cd seeding bug, or tcsh's `cwdcmd`-pipe restriction — both above), drive a real subprocess through `exec.Command` with a stub `envoke` binary on `PATH` and assert on what it was called with *and* that any side effect (a `setenv`/`export`/`set` the stub emits) actually persists in the calling shell — a test that only checks the stub was invoked can pass even when the real bug (a dropped pipe, a forked subshell) is present. All five shells now have this behavioral coverage (`TestGenerate_*HookFiresOnFirstCd`/`*SetenvPersistsInCallingShell` in `internal/shellinit/shellinit_test.go`) — a prior version of this note claimed only bash/zsh/tcsh did, which was inaccurate: zsh had no behavioral test at the time, only content/syntax checks, exactly like fish and powershell. `t.Skip` if the interpreter isn't on the test machine's `PATH` — as of this writing fish and PowerShell aren't installed in the local dev environment this project has been built in, so their behavioral tests only actually run (rather than skip) in CI, via `.dagger`'s `test-shell-fish`/`test-shell-powershell` checks — both confirmed passing for real against real interpreters (see `security_audit.md`'s Finding 1 for the verification log). bash/zsh/tcsh's behavioral tests run locally on this machine as well as in CI.
- **Trust store location: `$XDG_DATA_HOME` (state), not `$XDG_CONFIG_HOME` (config).** README only specifies XDG support for the config file itself; trust records are runtime state, not config, so they follow the XDG Base Directory Specification's separate data-home convention. Not written in the README — if you change this, update it there too.

## Documentation site

A user-facing docs site (`mkdocs.yml` + `docs/`) is separate from this file and the README — README stays the GitHub-landing pitch, this file stays contributor-facing, and `docs/` is the structured Read the Docs-style site (Getting Started, Configuration, Trust Model, Debugging, Design Notes). Built with MkDocs Material (`docs/requirements.txt` pins the version) rather than Sphinx or Docusaurus — pure Markdown (no rST rewrite) and no Node toolchain in a repo that's otherwise zero-dependency. `docs/contributing.md` embeds the real `CONTRIBUTING.md` verbatim via `pymdownx.snippets` (`--8<-- "CONTRIBUTING.md"`) instead of duplicating it, so there's one source of truth; cross-file links inside `CONTRIBUTING.md` had to become absolute `github.com/Neirda24/envoke/blob/main/...` URLs instead of relative paths, since a relative `CLAUDE.md`/`README.md` link doesn't resolve once the file is rendered under `docs/`'s own path — `mkdocs build --strict` catches this class of broken link, verified locally in a scratch venv before this was committed.

- Preview locally: `pip install -r docs/requirements.txt && mkdocs serve` (live-reload at `localhost:8000`) — or, without installing Python at all, the `Docs` function in `.dagger/main.go`: `dagger -m ./.dagger call docs up --ports 8000:8000` runs the same `mkdocs serve` inside a container and tunnels port 8000 to the host. Its `docsBase()` helper installs `docs/requirements.txt` before the source tree is mounted, so that layer only invalidates on a requirements change, not on every doc edit. The service must be built via `AsService(dagger.ContainerAsServiceOpts{Args: [...]})`, not `WithExec(...).AsService()` — `WithExec` runs the command once during query resolution (its output shows up in build logs, which is misleading) rather than making it the service's persistent entrypoint, so the container never actually listens once `up` tries to tunnel to it.
- `.github/workflows/docs.yml` builds and deploys to GitHub Pages on push to `main` (paths-filtered to `docs/`, `mkdocs.yml`, `CONTRIBUTING.md`) via `actions/deploy-pages` — this is the one GitHub Actions workflow in the repo; it's scoped to docs deployment only and doesn't run the Go checks (that's still `dagger check -m .dagger`, on-demand). Requires the repo's Pages source (Settings → Pages) to be set to "GitHub Actions" once — not something this workflow can do for you.
- `mkdocs-material` announced maintenance mode in early 2026 (feature-frozen since 9.7.0, critical fixes only until Nov 2026) in favor of a future rewrite ("Zensical"). Fine for now given how small this docs site is; worth revisiting the tool choice if that timeline lapses without a stable successor.

## Don't

- Don't add a feature from the README's "Use cases" section before the MVP step it depends on is done.
- Don't add implicit/automatic leave behavior — re-read the "enter/leave are independent" principle above before adding anything that feels like convenience state management.
- Don't let a GitHub Release go out without install/upgrade instructions in its notes. `.goreleaser.yaml`'s `release.footer` template is the mechanism — it's appended after the auto-generated changelog on every release, so this is automatic, not something to add by hand per release. If a new install method ships (Scoop, nfpm, the install script), update the footer template too, not just the README's Installation section.
